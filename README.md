# CURP in docker container

## How to use pre-built image

```shell
docker run flat35hd99/poc-curp
```

This will show help message by default. You can specify any commands like:

```shell
docker run flat35hd99/poc-curp curp compute ...
```
